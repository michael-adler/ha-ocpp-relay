version: 2
updates:
  # GitHub Actions
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"

  # Python dependencies (pip)
  - package-ecosystem: "pip"
    directory: "/"          # wherever your requirements.txt / pyproject.toml lives
    schedule:
      interval: "weekly"
